Work only from known code in Northern Shield. Do not invent APIs or assume file contents.

 KNOWN FILE STRUCTURE:
     src/main.js
     src/core/game.js
     src/core/renderer.js
     src/grid/grid.js
     src/entities/enemy.js
     src/entities/tower.js
     src/entities/bullet.js
     src/assets.js

 NOTE: engine/ contains old dead skeleton files — do not reference them.

 KNOWN EXPORTS:
     grid.js:         export const CELL, export class Grid
     enemy.js:       export const ENEMY_TYPES, ENEMY_DEFS, export class Enemy
     tower.js:       export const TOWER_TYPES, TOWER_DEFS, export class Tower
     bullet.js:     export class Bullet
     renderer.js: export { canvas, ctx }
     assets.js:     export const SPRITES
     game.js:         no exports (top-level side-effect module)

 KNOWN GAME STATE VARIABLES (in game.js module scope):
     grid, currentPath, enemies[], towers[], bullets[], particles[]
     gold, lives, slain, goldEarned, leaked
     buildMode, selectedTowerType, gameOver
     waveState, waveNumber, waveTimer, spawnQueue[]
     shakeX, shakeY

 KNOWN CONSTANTS (game.js):
     COLS=50, ROWS=30, CELL_SIZE=14, WALL_COST=5
     SIDEBAR_W=52, RIGHT_PANEL_W=188, GRID_LEFT=52, GRID_TOP=56
     SPAWN={col:0,row:15}, GOAL={col:49,row:15}
     STARTING_GOLD=85, STARTING_LIVES=20, MAX_LEVEL=10
     COUNTDOWN_FRAMES=150, BREAK_FRAMES=250
     BUILD_BTN, BUILD_ITEMS (derives from TOWER_DEFS)

 RULES:
 - If you need to know what's in a file, read it first
 - Do not invent new exported functions that don't exist yet
 - Do not assume a method exists on Grid, Enemy, Tower, or Bullet without checking
 - If information is missing, ask a concise question
 - Do not hallucinate game engine systems (there is no engine — it's vanilla JS)
