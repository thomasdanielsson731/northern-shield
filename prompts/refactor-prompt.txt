Refactor Northern Shield for clarity and maintainability.

 NAMING CONVENTIONS IN USE (do not change without reason):
     Variables: gold (not credits), slain (not kills), lives
     UI labels:     Gold / Lives / Slain (English)
     Enemy UI:     Graveborn / Wisp / Golem / Banshee
     Tower UI:     Shield Wall / Berserker / Valkyrie / Archer / Catapult / Blondie
     Internal ENEMY_TYPES keys: infantry, drone, tank, banshee
     Internal TOWER_TYPES keys: WALL, BERSERK, VALKYRIE, MILITARY, CATAPULT, BLONDIE
     Cell const: CELL.EMPTY / WALL / SPAWN / GOAL / TOWER

 CURRENT FILE SIZE (watch these):
     game.js: ~1400+ lines — intentionally large (integration layer), acceptable
     enemy.js: ~200+ lines — acceptable
     tower.js: ~500+ lines — larger due to sprite draw methods, acceptable
     grid.js: ~120 lines — fine
     bullet.js: ~75 lines — fine
     assets.js: ~30 lines — fine

 KNOWN CODE SMELLS (low priority):
     - engine/ folder contains dead skeleton files — safe to delete when convenient
     - drawHud split into drawTopBar/drawLeftSidebar/drawRightPanel/drawBottomBuildBar
         if any of these grow beyond ~80 lines, consider splitting further

 REFACTOR GOALS:
     - Reduce coupling only where it causes real friction
     - Improve naming when it causes confusion
     - Simplify control flow (no nested ternaries, no clever one-liners)
     - Remove duplication — but three similar lines is better than a premature abstraction
     - Preserve behavior exactly

 DO NOT:
     - Extract a class or module "for cleanliness" without a concrete reason
     - Change internal key names (infantry/drone/tank/banshee) — used in ENEMY_DEFS lookups
     - Rename the CELL constants
     - Add abstraction layers that make the code harder to follow for a solo dev
